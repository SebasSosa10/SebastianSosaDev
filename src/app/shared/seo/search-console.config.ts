/**
 * Tokens de verificación Search Console.
 * Si el valor es `null`, no se emite la meta (estructura lista para activar).
 */
export const SEARCH_CONSOLE = {
  googleSiteVerification: 'oqzDHBZp3TF-6n4QMzLXYBvHvNlIx6tho_J-Q0C5ASI' as
    | string
    | null,
  bingSiteVerification: null as string | null,
} as const;
