[build]
  command = "npm run build"
  publish = "dist/portafolio/browser"

[[plugins]]
  package = "@netlify/angular-runtime"

# SPA fallback solo para GET/HEAD — no interceptar POST de Netlify Forms
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  methods = ["GET", "HEAD"]
